# Supabase Configuration
# Get these values from your Supabase project dashboard
# Settings -> API -> Project URL and anon/public key

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Note: The Anthropic API key should be set as a Supabase Edge Function secret
# Do NOT add it here - it should never be exposed to the frontend
# Set it via Supabase dashboard: Edge Functions -> Secrets -> ANTHROPIC_API_KEY
