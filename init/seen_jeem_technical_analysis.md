# تحليل تقني شامل
## لعبة سين جيم (Seen Jeem)
### دليل المهندس البرمجي لبناء لعبة مشابهة

---

## 1. نظرة عامة على المنتج

سين جيم هي **لعبة ثقافية جماعية كويتية** تفاعلية مصممة للتجمعات العائلية والأصدقاء (الجمعات والدواوين). اللعبة متاحة عبر موقع إلكتروني وتطبيقات iOS و Android.

| العنصر | القيمة |
|--------|--------|
| الموقع الرسمي | seenjeemkw.com |
| المطور | Enzo Zid General Trading Co. WLL |
| المنصات | iOS 16.4+ / Android 7.0+ |
| السوق المستهدف | الكويت (توسع للسعودية) |
| نموذج اللعب | فريقين متنافسين |

---

## 2. هيكل اللعبة الأساسي

### 2.1 الثوابت الرئيسية

| العنصر | القيمة | الوصف |
|--------|--------|-------|
| عدد الفئات | **6 فئات** | في كل لعبة |
| عدد الأسئلة | **36 سؤال** | 6 أسئلة لكل فئة |
| اختيار الفئات | **3 + 3** | كل فريق يختار 3 فئات |
| وسائل المساعدة | **3-4 وسائل** | لكل فريق |
| مكتبة الفئات | **+25 فئة** | متاحة للاختيار |

### 2.2 آلية اختيار الفئات

```
┌─────────────────────────────────────────────────────┐
│                  مرحلة الاختيار                      │
├─────────────────────────────────────────────────────┤
│  1. الفريق الأول يختار 3 فئات من المكتبة            │
│  2. الفريق الثاني يختار 3 فئات مختلفة              │
│  3. المجموع: 6 فئات × 6 أسئلة = 36 سؤال            │
│  4. الفئات تتنوع أسبوعياً مع إضافات جديدة           │
└─────────────────────────────────────────────────────┘
```

### 2.3 نظام النقاط

كل سؤال له قيمة نقاط مختلفة حسب مستوى الصعوبة:

| النقاط | مستوى الصعوبة | عدد الأسئلة لكل فئة |
|--------|---------------|---------------------|
| **100** | سهل | 2 أسئلة |
| **200** | متوسط | 2 أسئلة |
| **300** | صعب | 2 أسئلة |
| **500** | الأصعب | إضافي (اختياري) |

---

## 3. نظام وسائل المساعدة (Power-ups)

كل فريق يحصل على **3-4 وسائل مساعدة** يمكن استخدام كل واحدة **مرة واحدة فقط** خلال اللعبة:

### 3.1 اتصال بصديق 📞

| الخاصية | التفاصيل |
|---------|----------|
| **الوظيفة** | يسمح للفريق بالاتصال بشخص خارج اللعبة للمساعدة |
| **وقت الاستخدام** | ✅ بعد رؤية السؤال |
| **عدد الاستخدام** | مرة واحدة لكل فريق |
| **التأثير** | لا يؤثر على النقاط - فقط مساعدة |

### 3.2 الحفرة 🕳️

| الخاصية | التفاصيل |
|---------|----------|
| **الوظيفة** | إذا أجاب الفريق صحيحاً، تُخصم النقاط من الفريق المنافس |
| **وقت الاستخدام** | ⚠️ قبل رؤية السؤال (مخاطرة!) |
| **عدد الاستخدام** | مرة واحدة لكل فريق |
| **التأثير** | `opponent.score -= question.points` |

### 3.3 جاوب جوابين ✌️

| الخاصية | التفاصيل |
|---------|----------|
| **الوظيفة** | يسمح بتقديم إجابتين للسؤال الواحد |
| **وقت الاستخدام** | ✅ بعد رؤية السؤال |
| **عدد الاستخدام** | مرة واحدة لكل فريق |
| **التأثير** | فرصتين للإجابة الصحيحة |

### 3.4 استريح 😴

| الخاصية | التفاصيل |
|---------|----------|
| **الوظيفة** | اختيار لاعب من الفريق المنافس لمنعه من المشاركة |
| **وقت الاستخدام** | ✅ بعد رؤية السؤال |
| **عدد الاستخدام** | مرة واحدة لكل فريق |
| **التأثير** | إضعاف الفريق المنافس تكتيكياً |

---

## 4. تدفق اللعبة (Game Flow)

```
┌──────────────────────────────────────────────────────────────┐
│                        Game Flow Diagram                      │
└──────────────────────────────────────────────────────────────┘

    ┌─────────────┐
    │   START     │
    └──────┬──────┘
           │
           ▼
    ┌─────────────────┐
    │  إنشاء لعبة /   │
    │  إدخال كود      │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │  تسمية الفريقين │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ فريق 1 يختار    │
    │   3 فئات        │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ فريق 2 يختار    │
    │   3 فئات        │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │   GAME LOOP     │◄─────────────────┐
    │   (36 سؤال)     │                  │
    └────────┬────────┘                  │
             │                           │
             ▼                           │
    ┌─────────────────┐                  │
    │  عرض السؤال    │                  │
    │  + المؤقت       │                  │
    └────────┬────────┘                  │
             │                           │
             ▼                           │
    ┌─────────────────┐                  │
    │ استخدام وسيلة؟  │──Yes──►[Power-up]│
    └────────┬────────┘                  │
             │No                         │
             ▼                           │
    ┌─────────────────┐                  │
    │  الفريق يجيب    │                  │
    │   (شفهياً)      │                  │
    └────────┬────────┘                  │
             │                           │
             ▼                           │
    ┌─────────────────┐                  │
    │  كشف الإجابة    │                  │
    └────────┬────────┘                  │
             │                           │
             ▼                           │
    ┌─────────────────┐                  │
    │ تحديد من أجاب   │                  │
    │    صحيحاً       │                  │
    └────────┬────────┘                  │
             │                           │
             ▼                           │
    ┌─────────────────┐                  │
    │  تحديث النقاط   │                  │
    └────────┬────────┘                  │
             │                           │
             ▼                           │
    ┌─────────────────┐     More         │
    │ أسئلة متبقية؟   │──Questions──────►┘
    └────────┬────────┘
             │No
             ▼
    ┌─────────────────┐
    │  النتيجة       │
    │  النهائية      │
    └────────┬────────┘
             │
             ▼
    ┌─────────────┐
    │    END      │
    └─────────────┘
```

---

## 5. البنية التقنية المقترحة

### 5.1 نموذج البيانات (Data Models)

#### Game Session
```typescript
interface GameSession {
  gameId: string;           // معرف فريد للجلسة
  gameCode: string;         // كود الدخول (6 أحرف)
  status: 'lobby' | 'playing' | 'paused' | 'finished';
  teams: Team[];
  categories: Category[];
  currentQuestionIndex: number;
  currentTeamTurn: 0 | 1;
  createdAt: Date;
  hostId: string;
}
```

#### Team
```typescript
interface Team {
  id: string;
  name: string;
  score: number;
  powerUps: {
    callFriend: { used: boolean; };
    pit: { used: boolean; };
    doubleAnswer: { used: boolean; };
    rest: { used: boolean; };
  };
  selectedCategories: string[];  // 3 category IDs
}
```

#### Category
```typescript
interface Category {
  id: string;
  name: string;              // مثال: "كرة القدم العالمية"
  icon: string;              // emoji أو رابط أيقونة
  questions: Question[];     // 6 أسئلة
  isKids: boolean;           // فئة أطفال؟
  region: 'kw' | 'sa' | 'all';
}
```

#### Question
```typescript
interface Question {
  id: string;
  questionText: string;
  correctAnswer: string;
  points: 100 | 200 | 300 | 500;
  difficulty: 'easy' | 'medium' | 'hard' | 'expert';
  categoryId: string;
  mediaUrl?: string;         // صورة أو صوت اختياري
}
```

### 5.2 State Machine للعبة

```typescript
enum GameState {
  LOBBY = 'lobby',
  TEAM1_SELECTING = 'team1_selecting',
  TEAM2_SELECTING = 'team2_selecting',
  QUESTION_DISPLAY = 'question_display',
  AWAITING_ANSWER = 'awaiting_answer',
  POWERUP_ACTIVE = 'powerup_active',
  REVEAL_ANSWER = 'reveal_answer',
  SCORE_ASSIGNMENT = 'score_assignment',
  GAME_OVER = 'game_over'
}

// State Transitions
const transitions = {
  [GameState.LOBBY]: ['TEAM1_SELECTING'],
  [GameState.TEAM1_SELECTING]: ['TEAM2_SELECTING'],
  [GameState.TEAM2_SELECTING]: ['QUESTION_DISPLAY'],
  [GameState.QUESTION_DISPLAY]: ['AWAITING_ANSWER', 'POWERUP_ACTIVE'],
  [GameState.POWERUP_ACTIVE]: ['AWAITING_ANSWER'],
  [GameState.AWAITING_ANSWER]: ['REVEAL_ANSWER'],
  [GameState.REVEAL_ANSWER]: ['SCORE_ASSIGNMENT'],
  [GameState.SCORE_ASSIGNMENT]: ['QUESTION_DISPLAY', 'GAME_OVER'],
};
```

### 5.3 Power-up Logic

```typescript
// الحفرة (قبل السؤال)
function usePit(game: GameSession, teamIndex: number): void {
  const team = game.teams[teamIndex];
  if (team.powerUps.pit.used) throw new Error('Already used');
  
  team.powerUps.pit.used = true;
  team.powerUps.pit.activeForQuestion = game.currentQuestionIndex;
  // إذا الإجابة صحيحة: opponent.score -= question.points
}

// اتصال بصديق (بعد السؤال)
function useCallFriend(game: GameSession, teamIndex: number): void {
  const team = game.teams[teamIndex];
  if (team.powerUps.callFriend.used) throw new Error('Already used');
  
  team.powerUps.callFriend.used = true;
  // بدء مؤقت 60 ثانية للاتصال
  startCallTimer(60);
}

// جاوب جوابين (بعد السؤال)
function useDoubleAnswer(game: GameSession, teamIndex: number): void {
  const team = game.teams[teamIndex];
  if (team.powerUps.doubleAnswer.used) throw new Error('Already used');
  
  team.powerUps.doubleAnswer.used = true;
  // السماح بإجابتين
  game.allowedAnswers = 2;
}

// استريح (بعد السؤال)
function useRest(game: GameSession, teamIndex: number, targetPlayer: string): void {
  const team = game.teams[teamIndex];
  if (team.powerUps.rest.used) throw new Error('Already used');
  
  team.powerUps.rest.used = true;
  // منع اللاعب المختار من المشاركة
  game.excludedPlayer = targetPlayer;
}
```

---

## 6. نموذج العمل (Business Model)

### 6.1 نظام الباقات

| الباقة | الوصف | السعر التقريبي |
|--------|-------|----------------|
| تجريبية | لعبة واحدة مجانية | مجاني |
| فردية | لعبة واحدة | ~1-2 د.ك |
| باقة 5 | 5 ألعاب | خصم 10-15% |
| باقة 10 | 10 ألعاب | خصم 20% |

### 6.2 مصادر الدخل

```
┌─────────────────────────────────────────────────┐
│              Revenue Streams                     │
├─────────────────────────────────────────────────┤
│  💰 Pay-per-play (الدفع لكل لعبة)               │
│  🎁 Gift Codes (أكواد الهدايا)                  │
│  📱 In-app purchases                            │
│  🤝 Sponsorships (رعاة مثل ذا شفز)              │
│  🏆 Paid Tournaments (بطولات مدفوعة)            │
│  📺 TV Show Integration (برنامج تلفزيوني)       │
└─────────────────────────────────────────────────┘
```

### 6.3 استراتيجية المحتوى

- ✅ إضافة فئات جديدة **أسبوعياً**
- ✅ فئات موسمية (رمضان، أعياد)
- ✅ **سين جيم كيدز** للأطفال (6-10 سنين)
- ✅ فئات محلية (كويت، سعودية)
- ✅ فئات متخصصة (قرآن، إسلاميات، رياضة، فن)

---

## 7. توصيات واجهة المستخدم

### 7.1 الشاشات الرئيسية

```
┌────────────────────────────────────────────────────────────┐
│                    Screen Flow                              │
├────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌─────────┐    ┌──────────┐    ┌───────────────┐         │
│   │  Home   │───►│  Setup   │───►│   Category    │         │
│   │ Screen  │    │  Teams   │    │   Selection   │         │
│   └─────────┘    └──────────┘    └───────┬───────┘         │
│                                          │                  │
│                                          ▼                  │
│   ┌─────────┐    ┌──────────┐    ┌───────────────┐         │
│   │ Results │◄───│  Answer  │◄───│   Question    │         │
│   │ Screen  │    │  Reveal  │    │   Display     │         │
│   └─────────┘    └──────────┘    └───────────────┘         │
│                                                             │
└────────────────────────────────────────────────────────────┘
```

### 7.2 عناصر UI المهمة

| العنصر | الوصف | الموقع |
|--------|-------|--------|
| **Scoreboard** | لوحة النقاط دائمة الظهور | أعلى الشاشة |
| **Timer** | عداد زمني تنازلي | وسط/أعلى |
| **Power-up Icons** | مؤشرات الوسائل المتاحة | جانب كل فريق |
| **Question Card** | بطاقة السؤال | وسط الشاشة |
| **Team Buttons** | أزرار تحديد الفائز | أسفل الشاشة |

### 7.3 اعتبارات RTL

```css
/* Critical RTL Support */
html {
  direction: rtl;
}

.game-container {
  text-align: right;
  font-family: 'Tajawal', 'Arial', sans-serif;
}

.scoreboard {
  flex-direction: row-reverse;
}
```

---

## 8. التقنيات المقترحة

### 8.1 Tech Stack Recommendations

| المكون | الخيار الأول | الخيار البديل |
|--------|-------------|---------------|
| **Frontend Web** | Next.js + React | Vue.js + Nuxt |
| **Mobile Apps** | React Native | Flutter |
| **Backend** | Node.js + Express | Python FastAPI |
| **Database** | Firebase Firestore | Supabase |
| **Real-time Sync** | Firebase Realtime | Socket.io |
| **Authentication** | Firebase Auth | Auth0 |
| **Payments (Kuwait)** | MyFatoorah / Tap | Stripe |
| **Hosting** | Vercel / Firebase | AWS / GCP |

### 8.2 Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                      System Architecture                         │
└─────────────────────────────────────────────────────────────────┘

    ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
    │   Web App    │     │  iOS App     │     │ Android App  │
    │  (Next.js)   │     │(React Native)│     │(React Native)│
    └──────┬───────┘     └──────┬───────┘     └──────┬───────┘
           │                    │                    │
           └────────────────────┼────────────────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │   API Gateway         │
                    │   (Firebase/Express)  │
                    └───────────┬───────────┘
                                │
           ┌────────────────────┼────────────────────┐
           │                    │                    │
           ▼                    ▼                    ▼
    ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
    │  Firestore  │     │  Realtime   │     │   Auth      │
    │  Database   │     │  Database   │     │   Service   │
    └─────────────┘     └─────────────┘     └─────────────┘
           │
           ▼
    ┌─────────────┐
    │  Cloud      │
    │  Functions  │
    └─────────────┘
```

---

## 9. الميزات الإضافية

### 9.1 نظام البطولات

```typescript
interface Tournament {
  id: string;
  name: string;
  teams: Team[];
  format: 'knockout' | 'roundRobin' | 'groups';
  matches: Match[];
  prizes: Prize[];
  startDate: Date;
  status: 'upcoming' | 'ongoing' | 'finished';
}
```

### 9.2 نظام الهدايا

```typescript
interface GiftCode {
  code: string;           // مثال: "SEENJEEM-ABC123"
  type: 'single' | 'pack';
  gamesCount: number;
  purchasedBy: string;
  redeemedBy?: string;
  createdAt: Date;
  expiresAt: Date;
}
```

### 9.3 التكامل التلفزيوني

- 📺 برنامج **"ياهلا سين جيم"** على تلفزيون الكويت
- 🕐 يُبث يومياً في رمضان الساعة 1 فجراً
- 🏆 فرق حقيقية تتنافس بنفس نظام اللعبة

---

## 10. ملخص للمطور

### ✅ النقاط الأساسية لبناء لعبة مشابهة:

| # | المتطلب | التفاصيل |
|---|---------|----------|
| 1 | **نظام الفرق** | فريقين يتبادلان الأدوار |
| 2 | **الأسئلة** | 6 فئات × 6 أسئلة = 36 سؤال |
| 3 | **وسائل المساعدة** | 4 وسائل بقواعد مختلفة (قبل/بعد السؤال) |
| 4 | **نظام النقاط** | متدرج حسب الصعوبة (100-500) |
| 5 | **نموذج الدفع** | دفع لكل لعبة + لعبة مجانية للتجربة |
| 6 | **المحتوى** | إضافة فئات جديدة أسبوعياً |
| 7 | **التوطين** | دعم RTL والثقافة المحلية |
| 8 | **المنصات** | ويب + موبايل |

### 🎯 Minimum Viable Product (MVP)

```
MVP Features:
├── ✅ إنشاء لعبة بكود
├── ✅ فريقين + تسمية
├── ✅ اختيار 6 فئات (3 لكل فريق)
├── ✅ 36 سؤال متسلسل
├── ✅ نظام نقاط
├── ✅ وسيلتين على الأقل (الحفرة + اتصال صديق)
├── ✅ شاشة النتائج
└── ✅ دعم RTL
```

---

## 📚 مصادر إضافية

- **الموقع الرسمي:** [seenjeemkw.com](https://seenjeemkw.com)
- **التطبيق:** متوفر على App Store و Google Play
- **حسابات التواصل:** @seenjeemkw / @SeenJeemSA

---

*تم إعداد هذا التقرير التقني لأغراض التعلم والتطوير*

